function sumOfTwoNumbers(firstNumber:number,secondNumber:number):number{
    return firstNumber+secondNumber;
}
const a: number=5;
const b: number=16;

const result =sumOfTwoNumbers(a,b);


console.log(`el valor de la suma de (${a},${b}) es : ${result}`);
